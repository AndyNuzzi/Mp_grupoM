package Client;

public class CheckComments extends ClientOperation{
    public CheckComments (Client client, Controller.Controller controller){
        super ();
        // me traigo los datos del cliente
    }

    public boolean doOperation() {
        // llamar al controller
        return false;
    }
}
