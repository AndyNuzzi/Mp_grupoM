package Client;

public class AddPossibleSwindler extends AdministratorOperation{
    @Override
    public boolean doOperation() {
        // pasar la lista de clientes
        // llamar al controlador pasandole el id del posible swindler que se quiere añadir
        return true;
    }
}
